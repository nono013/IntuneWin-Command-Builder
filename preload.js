// Fichier preload pour la sécurité Electron
// Ce fichier s'exécute avant le chargement de la page web
// Il permet de contrôler ce qui est exposé au renderer process

window.addEventListener('DOMContentLoaded', () => {
    console.log('Application Electron chargée');
});